extend ../layout
block content 
  h1= article.title
  if user
    h5 Writen by #{author}
  p=article.body
  hr
  if user && user.id == article.author
    a.btn.btn-primary.me-2(href='/articles/'+article._id+'/edit') Edit
    a.btn.btn-danger.delete-article(hrf="#",data-id=article._id) Delete